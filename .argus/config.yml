language: python
build:
  command: "uv sync --frozen --all-groups --all-packages --all-extras"
test:
  command: "uv run pytest tests/test_types.py -q --tb=short -x --no-header"
loop:
  max_iterations: 3
